# Process raw directory data to standard schema

Takes raw combined directory data from get_raw_directory() and
standardizes column names, types, and format.

## Usage

``` r
process_directory(raw_data)
```

## Arguments

- raw_data:

  Raw data frame from get_raw_directory()

## Value

Processed data frame with standard schema
