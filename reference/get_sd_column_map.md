# Get column mapping for SD DOE enrollment data

Returns a list mapping raw SD DOE column names to standardized names. SD
DOE uses varying column names across years.

## Usage

``` r
get_sd_column_map(end_year)
```

## Arguments

- end_year:

  School year end

## Value

Named list of column mappings
